"""FERE Engine package.

Contains small CLI entry-points that wrap the existing pipeline scripts so users can run the
pipelines with a single command (``fere-general`` / ``fere-tech`` / ``fere-tech-all``).
""" 